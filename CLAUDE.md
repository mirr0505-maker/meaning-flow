# CLAUDE.md — Meaning Flow 작업 규칙

이 파일은 Claude Code가 이 저장소(`e:\meaningflow`)에서 작업할 때 **반드시 따라야 하는 지침**이다.
서브에이전트 위임 없이 메인 세션이 코드 작성·검증·카피·다국어·안전성 검토까지 직접 수행한다.

**제품 명세·진척·기술스택·로드맵은 [`blueprint.md`](blueprint.md) 를 참조한다.**
원본 1차 소스는 다음 두 문서다:
- [`MeaningFlow_PRD_v1.1.docx`](MeaningFlow_PRD_v1.1.docx) — 제품 요구사항 (v1.1 글로벌 MVP)
- [`MeaningFlow_UserGuide_v1.1.docx`](MeaningFlow_UserGuide_v1.1.docx) — 사용자 가이드 (정서·톤 기준)

작업 단위는 `blueprint.md` 10장(로드맵)의 **Phase / STEP** 단위.

---

## 절대 수칙

### 0. Human Readable 원칙 (대전제)
모든 코드·카피는 훗날 사람(개발자 + 사용자)이 혼자 읽고 이해할 수 있어야 한다.
- 변수명·함수명은 역할이 명확히 드러나게 짓는다. 한국어 주석을 적극 사용.
- 복잡한 로직에는 **왜(Why)** 그렇게 작성했는지 한 줄 주석을 단다.
- 마법 숫자·의미 없는 축약 변수명(`a`, `tmp`, `d2`) 금지.
- 새 기능 블록 상단에 `// 🚀 기능명: 한 줄 설명` 형식으로 목적을 명시한다.

### 1. 코드 부풀리기 금지 (최우선)
- **요청한 것만 만든다.** PRD/blueprint 범위 밖의 기능·추상화·옵션을 임의로 추가하지 않는다.
- 요청 없는 리팩터링 금지. "겸사겸사" 다른 코드를 손대지 않는다.
- 불필요한 주석·docstring·헬퍼·설정 옵션을 덧붙이지 않는다.
- 미래를 위한 추측성 코드("나중에 필요할 수도") 금지.
- 라이브러리·의존성을 함부로 늘리지 않는다. 추가가 필요하면 먼저 이유를 보고한다 (`blueprint.md` 6장 기술스택 기준).
- 같은 일을 더 적은 코드로 할 수 있으면 그쪽을 택한다.

### 2. 코드 보호 — 디자인 프로토타입 보존
현재 `src/*.jsx` + `tokens.css` + `Meaning Flow.html` 은 **Phase 0 흐름 검증을 통과한 디자인 프로토타입**이다.
- 요구사항과 무관한 **기존 코드를 절대 수정하지 않는다.**
- 디자인 토큰(`tokens.css`) · 컴포넌트 구조 · 시각 톤을 임의로 바꾸지 않는다.
- Phase 1(React Native 마이그레이션) 진입 시에도 이 프로토타입을 **참조용 원본**으로 남긴다 (덮어쓰기 금지).
- 변경이 필요하면 `blueprint.md` 디자인 원칙(7장)과 충돌하지 않는지 먼저 확인한다.

### 3. 선 보고 후 실행
- 코드 수정 전 **AS-IS → TO-BE**를 한국어로 설명하고 사용자 확인을 받는다.
- PRD/blueprint 범위를 벗어나는 작업은 먼저 사용자에게 확인한다.
- 외부 API(Supabase·DeepL·OpenAI Moderation 등) 신규 호출 코드를 만들 때는 비용·한도·실패 시 폴백을 함께 보고한다.

### 4. Surgical Edit
- 파일 전체 재작성 대신 **필요한 부분만** Edit 도구로 정밀 수정한다.
- 신규 파일이 아니면 전체 덮어쓰기(Write) 금지.

### 5. 200라인 규칙
- 한 파일이 200라인을 초과하면 기능별 분리를 먼저 제안한다.
- 단, 디자인 프로토타입의 기존 파일(`flow-screens.jsx` 681줄 등)은 보존 대상 — 신규 작업물에만 적용.

---

## Meaning Flow 도메인 절대 원칙 (위반 금지)

이 앱은 IN(내향 직관형) 사용자의 정서 안전이 가장 중요한 요구사항이다.
일반 To-Do 앱의 관행을 그대로 가져오면 사용자가 다친다.

### M1. 통계·압박·완벽주의 회로 차단
- **스트릭(연속 일수) 코드 작성 금지.** 끊김의 죄책감은 IN의 이탈 1순위 원인.
- **달성률·완료 카운터·진행 막대 금지.** 의미 일기 25%, 공명방 30% 같은 사용자용 KPI 표시 금지 (개발자 분석용 PostHog 만 허용).
- **붉은 알림 배지·미완료 강조 색상 금지.** (`blueprint.md` 7.1 팔레트 참조)
- **2분 마이크로 변환기** · **70% 만족 버튼**은 완벽주의 회로 차단 장치 — 비활성화·축소·기본 OFF 금지.
- **70% 완료 버튼이 더 눈에 띄게.** 디자인 의도. UI에서 100% 완료를 시각적으로 우대하면 안 된다.

### M2. 카피라이팅 원칙 (PRD 7.2)
모든 사용자 노출 문자열은 다음을 따른다.
- **명령형 금지** — "하세요" 대신 "해볼까요?" "어떠세요?" 같은 질문형.
- **숫자 강조 금지** — "오늘 3개 완료!" 같은 카운터형 카피 금지. 한 줄 회고형으로.
- **두 자아 인정** — "주/부 MBTI" 같은 위계 표현 금지. "혼자일 때의 나 / 함께일 때의 나" 만 사용.
- **다국어** — 단순 기계 번역 결과를 출고용으로 박지 않는다. 정서적 결을 살린 모국어 카피로 검수.
- **빈 화면을 죄책감으로 만들지 않는다** — "오늘은 할 일이 없네요" 같은 공백 카피는 따뜻하게.

### M3. 공명방(Resonance Garden) 안전 원칙 (PRD 3.3 · 4.5)
공명방은 IN 정서에 가장 위험한 영역이므로 다음을 절대 어기지 않는다.
- **댓글 기능 절대 추가 금지.** 방향성: 단방향·무피드백.
- **좋아요·평가 시스템 금지.** 오직 `🌿 공명해요` 하나의 동조 액션만.
- **팔로워·팔로잉 개념 금지.** 데이터 모델·UI 어디에도 등장하면 안 된다.
- **실명·프로필 사진·정확한 게시 시각 노출 금지.** MBTI 조합 닉네임 + 대략적 시간("오늘 저녁", "어제 밤")만.
- **무한 스크롤 금지.** 한 번에 20개씩 + "더 보기" 버튼 (PRD 7.3).
- **공명한 사람이 누구인지 작성자에게 노출 금지.** 합계 숫자만.
- **게시 토글 기본 OFF.** 의도적 누름만 게시. 기본 ON 으로 바꾸는 PR 금지.

### M4. 모더레이션 + 위기 자원 의무
- 공명방 게시 전 **OpenAI Moderation API 통과를 우회·삭제하는 코드 금지.**
- 모더레이션 차단 시 부드러운 안내 카피로: "이 글은 다른 분께 닿기 어려울 것 같아요. 한 번 다시 봐주세요." (작성자 일기 자체는 본인에게 남아야 한다 — 글 자체를 지우지 말 것)
- **자해 암시 키워드·신고**에는 위기 자원 안내(국가별 상담 핫라인)를 **자동·우선** 노출. 이 흐름을 비활성화하거나 뒤로 미루는 변경 금지.

### M5. 다국어 — 원문 우선 + 온디맨드 번역 (PRD 3.4)
- 공명방 게시물 자동 전체 번역 금지. **원문 그대로** 표시가 기본.
- 번역은 사용자가 `🌍 번역` 버튼을 누를 때만 호출.
- **번역 결과는 `translations` 테이블에 영구 캐싱.** 같은 글을 다른 사용자가 번역해도 추가 API 호출 0.
- 사용자당 월 100회 번역 한도. 한도 우회·완화 금지.
- 번역 API 우선순위: **DeepL → Google Translate 폴백.** 키 없을 때도 앱은 동작해야 한다 (원문만 표시).
- **MVP 출고 언어 (2026-05-22 변경)**: 한국어 · 영어 · **일본어** 3개. 프·독은 키 자리만 마련(빈 객체), 출고는 Phase 5+ 별도 검수 후.
- **일본어 출고 게이트**: 현재 `ja.json` 은 메인 세션 1차 초안. **베타(Phase 4) 진입 전 일본 native speaker 검수 통과해야 출고.** 빠질 시 베타 전 정서 톤 부적합으로 사용자 이탈 가능. 검수 트래킹은 `blueprint.md` 11.0-F 별도 트랙.

### M6. 데이터 프라이버시 (PRD 5.2)
- `reflections`(의미 일기) — 본인만 read/write. 다른 사용자/관리자 노출 코드 금지.
- 공명방 게시 시 **작성자 식별자 분리** — `resonance_posts.user_id` 는 작성자 본인 조회용. 다른 사용자 응답 페이로드에 포함 금지.
- `resonances`(공명 액션) — 본인이 누른 것만 본인 view. 작성자에게는 합계만.
- 비밀키(SUPABASE / DEEPL / OPENAI 등)를 코드에 박지 않는다. 환경변수 + `expo-constants` / Edge Function secret 경유.

### M7. 면책·안내 문구 (전 화면 공통)
공명방 진입 화면에 다음과 유사한 안내가 상시 노출되어야 한다.
> 공명의 정원은 익명 광장입니다. 위급한 상황에서는 전문 기관과 연결되어야 합니다 — 자해/타해 생각이 들 때는 본문의 위기 자원을 먼저 확인해 주세요.

---

## 기능 추가 시 검증 의무 (필수 절차)

코드를 새로 만들거나 고치면, **반드시 아래 검증을 거친 뒤** 완료로 본다.
검증 없이 "완료"라고 보고하지 않는다.

1. **PRD/UserGuide 정합성 점검.** 변경이 PRD 8장(MVP 범위)에 포함되는지, 8.2(MVP 제외) 항목을 끌어다 쓰지 않는지 확인.
2. **도메인 원칙 통과 점검.** 위 M1~M7 중 하나라도 어기는지 자가 점검 후 보고.
3. **검증 테스트를 함께 만든다.** 새 기능에는 그 기능을 확인하는 테스트를 같이 작성한다. 코드만 만들고 끝내지 않는다.
   - Phase 0(현 디자인 프로토타입): 브라우저 수동 확인 + 스크린샷.
   - Phase 1+(RN/Expo): Jest + React Native Testing Library.
4. **외부 API 의존 코드는 가짜 데이터 검증을 붙인다.** Supabase·DeepL·OpenAI Moderation 호출 코드는 키 없는 환경에서도 파서·UI 로직을 검증할 수 있는 테스트를 만든다.
5. **카피 변경은 5개 언어 모두 검수 대상**(한·영·일·불·독). MVP 단계는 한·영만 출고하지만 i18n 키는 5개 언어 자리를 먼저 마련.
6. **검증을 실제로 실행한다.**
7. **엣지 케이스 점검** — 빈 일기, 200자 초과, 모더레이션 실패, 번역 한도 초과, 오프라인, 시간대 경계(06/10/18/22시).
8. 검증 통과해야 STEP 완료. 실패하면 수정 후 다시 검증.

---

## 작업 흐름

- 모든 작업은 메인 세션이 직접 수행한다 (서브에이전트 위임 없음).
- 한 STEP이 검증 완료되기 전 다음 STEP으로 넘어가지 않는다.
- 카피·다국어·안전성 검토는 위 M1~M7 + `blueprint.md` 7장 기준으로 메인이 직접 점검한다.
- 진척 변경 시 [`blueprint.md`](blueprint.md) 11장(현재 진척)을 같이 갱신한다.

---

## 금지 사항

- `Write` 도구로 기존 파일 전체 덮어쓰기 (신규 파일 제외)
- 디자인 프로토타입(`src/*.jsx`, `tokens.css`, `Meaning Flow.html`) 임의 수정 — Phase 0 결과물 보존
- 요청 없는 리팩터링, 불필요한 주석·코드·의존성 추가
- 검증·도메인 원칙 점검 없이 기능을 "완료"로 보고
- 스트릭·달성률·붉은 알림 배지 코드 작성
- 공명방에 댓글·좋아요·팔로워·실명·정확 시각 노출 추가
- 모더레이션·위기 자원 흐름 우회·축소
- 공명방 자동 전체 번역 (원문 우선 원칙 위반)
- 의미 일기·공명방 페이로드에 작성자 식별자 노출
- `git push` / 배포 자동 실행 (사용자 명시 요청 시에만)
