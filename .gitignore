# Node / npm
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
.DS_Store
Thumbs.db
*.pem

# Local env (안전망 — app/.gitignore 에 이미 막혀있지만 루트에도)
.env
.env.*.local

# IDE
.vscode/
.idea/
*.swp

# Claude Code local settings (personal)
.claude/

# iOS crash reports (개인정보 가능성 + 임시 파일)
*.ips

# Expo build logs (임시)
**/export-log.txt

# Root level expo fragment (e:\meaningflow\app/app.json 만 사용)
/app.json

# Build output
dist/
build/
*.tsbuildinfo

# Temporary extracts
_MeaningFlow_*.txt

# Expo / Metro
.expo/
.metro-health-check*

# Apple Sign In / 기타 비밀키 (절대 git 에 올리지 말 것)
secrets/
*.p8
